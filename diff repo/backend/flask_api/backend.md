Flask app idea

for one route 
So when the client loads up to the backend, the Flash server should send two pieces of information. One is the user data, the user profile, and the other is the data for displaying on the data log. These are the two things that should be sent for one route, which is accessed when the React app is loaded first.


for next route 
So, whenever a user inputs anything in the chat UI, it takes in the input and responds with the generic code.


bot 
There should be a bot for creating data randomly under different categories like fraud alerts, bank balance, bank debit/credit alerts, and policy updates.
which 


